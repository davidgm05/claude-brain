---
project: claude-mem-observer-sessions
date: 2026-04-23
session_id: a4280641-e335-4887-9443-ecf4cec44a1c
summary: created without properly unmounting it first
topics: [claude-mem-observer, problem-solution, re-initialization, react-leaflet, session]
tools: [react]
entities: [claude]
---
summary: created without properly unmounting it first
topics: [claude-mem-observer, problem-solution, re-initialization, react-leaflet, session]
tools: [react]
entities: [claude]

### Observed Session

### Assistant (thinking)
<details><summary>View thinking</summary>

The user is encountering an error "Map container is already initialized" in their React Leaflet application. This is a runtime error occurring in the Map.js file when trying to initialize a map container.

Let me analyze what this error tells us:

1. The error is: "Map container is already initialized"
2. It occurs in: `../src/map/Map.js (1094:10)` - specifically in `_initContainer`
3. It's triggered from: `node_modules/react-leaflet/lib/MapContainer.js (30:25)`

This is a common issue in React Leaflet when:
- A MapContainer component is mounted multiple times
- The map is being recreated without properly unmounting it first
- Hot module reloading is causing double initialization
- There's a strict mode issue where components are rendered twice

This appears to be an active debugging session in the primary Claude session. I should record this as a bug discovery/fix in progress.

Let me create an observation for this:


</details>

### Assistant


```xml
<observation>
  <type>discovery</type>
  <title>React Leaflet Map Initialization Error</title>
  <subtitle>Map container initialization conflict identified in react-leaflet MapContainer component</subtitle>
  <facts>
    <fact>Error: "Map container is already initialized" occurs during map container setup</fact>
    <fact>Stack trace shows failure in Map.js _initContainer method at line 1094</fact>
    <fact>React Leaflet MapContainer is triggering the duplicate initialization via Class.js apply</fact>
    <fact>Error occurs when MapContainer is mounted multiple times or not properly unmounted before re-initialization</fact>
  </facts>
  <narrative>Encountered a runtime error in a React Leaflet application where the map container is being initialized multiple times. The error originates from the Leaflet Map.js _initContainer method being called on a container that has already been initialized. This commonly happens when a MapContainer component is rendered multiple times without proper cleanup, during hot module reloading with React strict mode (double invocation), or when component state changes trigger remounting of the map. The fix typically involves ensuring proper cleanup in useEffect with map.remove() or using key prop to force remount control.</narrative>
  <concepts>
    <concept>gotcha</concept>
    <concept>problem-solution</concept>
  </concepts>
  <files_read>
    <file>src/map/Map.js</file>
    <file>node_modules/react-leaflet/lib/MapContainer.js</file>
  </files_read>
  <files_modified/>
</observation>
```
